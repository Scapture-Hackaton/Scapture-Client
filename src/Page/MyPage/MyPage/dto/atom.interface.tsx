export interface userData {
  endDate: string | null;
  image: string | null;
  location: string | null;
  name: string;
  role: string;
  team: string | null;
}

export interface bananaData {
  balance: number;
  subscribed: boolean;
}
