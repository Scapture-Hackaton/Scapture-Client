export interface userData {
  endDate: string | null;
  image: string | null;
  location: string;
  name: string;
  role: string;
  team: string;
}

export interface bananaData {
  balance: number;
  subscribed: boolean;
}

export interface subscribedData {
  subscribed: boolean;
}
