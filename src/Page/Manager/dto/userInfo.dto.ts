export interface UserInfoDTO {
  userId: string;
  name: string;
  phoneNumber: string;
}

export interface UserDetailDTO {
  gender: string;
  name: string;
  brithYear: string;
  phoneNumber: string;
}
